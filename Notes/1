## Immediate TODO
[] Re-implement command buffer command submission (you're freeing them too frequently instead of taking a more common approach)

[] Accommodate the v2 shaders:
	[] New Vertex and Index Buffers
	[] Descriptor Set Types
	[] Descriptor Layouts
	[] Storage Buffers
	[] Compute Shaders
	[] New Push Constant
		- VkPushConstants
		- VkComputePushConstants
	[] New Layout Attributes
	[] Check yo' pipeline layout's layouts

## OVERALL
[] Make gltf (prefer glb) the primary model loader
[] Materials
	[] Material UBO
	[] Add to frag shader
	[] Maybe create another pipeline with shader modules that support mat's
	[] Include materials with gltf exports

## Side-Quests
[](1) Implement a Compute Fence
[](0) Timing information. See VkRenderer::draw()
[](0) Vertex count for gameData
[](0) Other gameData things. Might help you wrap your head around what needs to go where

# Checkin' out the Tut
- Update Location 1: SSBO - VkRenderer.cpp - mNodeTransFormData
- Update Location 2: SSBO - VkRenderer.cpp - mWorldTransformData

# Notes
 - Vertex attributes are in the pipeline creation
 - Instances populate the NodeTransform

# Architecture Quirks
 - Command pools are created by the EngineDevice, stored in VkRenderData. Commands are created by the CommandBuffer class.

11/01/25 - 
	1. Learned how vertex/index buffers are mapped once per model, used per instance
	2. Learned how single-shot command buffers work and that my architecture sucks.
	3. Found out that the screen stuttering might be caused by using vkFreeCommandBuffers too frequently instead of reusing them OR because I'm using vkQueueWaitIdle during command recording (Update: Unlikely, because this is only being performed during single-shot commands. Be sure to use fences during the main loop, and avoid single shots)
		see: https://chatgpt.com/g/g-p-67fd00e1b6748191913d770a83c124e5/c/69069bbb-bc4c-8330-8ba1-3f188f940760
10/30/25 - 
	1. Added Compute Shader Pipeline
	2. Added Another Command Pool for Compute Commands
	3. Added Command Buffer Creation for Compute
10/28/25 - Learned about how descriptors are attributed and bound. <aveng_model>
10/29/25 - Studying. Drafted v2 shaders. Added ModelManager.